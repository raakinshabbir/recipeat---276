<h2>Filtered Recipes</h2>

<ul>
  <% @recipes.each do |recipe| %>
    <li>
      <%= link_to recipe.name, recipe_path(recipe) %>  <!-- Link to recipe show page -->
    </li>
  <% end %>
</ul>

<%= link_to 'Back to Filter Recipes', filter_recipes_path %>  <!-- Link back to filter form -->
