<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Recipeat </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <!-- Swipe Button -->
          <li class="nav-item">
            <%= link_to 'Swipe Recipes', swipe_path, class: "btn btn-outline-success nav-link" %>
          </li>
          <!-- Add Recipe Button -->
          <li class="nav-item">
            <%= link_to 'Add Recipe', addrecipe_path, class: "btn btn-outline-primary nav-link" %>
          </li>
          <!-- Sign Out Button -->
          <li class="nav-item">
            <%= link_to 'Sign Out', destroy_user_session_path, 'data-turbo-method': :delete, class: "btn btn-outline-danger nav-link" %>
          </li>
        <% else %>
          <!-- Register Button -->
          <li class="nav-item">
            <%= link_to 'Register', new_user_registration_path, class: "btn btn-outline-danger nav-link" %>
          </li>
          <!-- Sign In Button -->
          <li class="nav-item">
            <%= link_to 'Sign In', new_user_session_path, class: "btn btn-outline-danger nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
